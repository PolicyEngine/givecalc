{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && bun install --frozen-lockfile && bun run build",
  "installCommand": "cd frontend && bun install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "frontend/.next"
}
