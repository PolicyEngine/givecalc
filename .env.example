# Modal configuration
# Set to "false" to run calculations locally (development mode)
# Set to "true" to use Modal for calculations (production)
USE_MODAL=true

# Modal authentication (required when USE_MODAL=true)
# Get these from: modal.com/settings
MODAL_TOKEN_ID=your_token_id_here
MODAL_TOKEN_SECRET=your_token_secret_here
